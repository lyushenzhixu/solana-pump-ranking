# SEO 规则

所有前端页面（HTML、模板、组件）必须遵循以下 SEO 规范：

## 必须满足

1. 每个页面有唯一的 `<title>`，格式为 `关键词 | Zhizhi Labs`，30-60 字符
2. 每个页面有 `<meta name="description">`，包含核心关键词，150 字符以内
3. 每个页面有 `<link rel="canonical">` 指向规范 URL
4. 每个页面有 Open Graph 标签（og:title, og:description, og:url, og:image, og:type, og:site_name）
5. 每个页面有 Twitter Card 标签
6. 每个页面有且仅有一个 `<h1>` 标签，包含品牌名或核心关键词
7. HTML 根元素设置 `lang="zh-CN"`
8. 使用语义化 HTML 标签（header, nav, main, article, section, footer）

## 禁止

1. 禁止使用纯客户端渲染（SPA）作为面向公众的页面
2. 禁止在 `robots.txt` 中添加 `Disallow: /`
3. 禁止使用未压缩的大图片
4. 禁止将 SEO 关键内容放在 JavaScript 动态加载中（搜索引擎爬虫无法执行 JS）

## 新增页面检查清单

- [ ] title 标签包含 "Zhizhi Labs"
- [ ] meta description 已设置
- [ ] canonical URL 正确
- [ ] OG + Twitter Card 标签完整
- [ ] 有且仅有一个 h1
- [ ] 已加入 sitemap（更新 `src/seo.js` 中的 staticPages）

## 参考

详见 `knowledge/seo-guidelines.md`
